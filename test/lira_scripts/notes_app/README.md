# Notes app (executable keyword DSL)

Portable app shape using F1–F7 features: signatures, return, bindings, member access, call/construct, constructor, interface.

```bash
npm run generate
npm test
```

Outputs land in `test/lira_dsl/notes_app/` and `test/lira_output/{ts,py}/notes_app/`.
