module notes_models

define export class Note
  define public readonly property id: string
  define public property title: string
  define private property body: string
  define constructor(id: string, title: string)
    set this.id = id
    set this.title = title
    set this.body = ""
  define public static method createEmpty() -> Note
    return construct Note("0", "")
